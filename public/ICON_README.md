# App Icon

Place your app icon here as `icon.png` (256x256 or larger recommended).

For Windows, you can also provide `icon.ico`.
For Linux, a PNG works best.

The icon will be used:
- As the application window icon
- In the installer/package
- In the system tray/dock
